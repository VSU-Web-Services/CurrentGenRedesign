Requirment for Conversion of site:

1. Do not apply XSL to index panels, only static pages. Index panels may have includes.
2. Display H1 or title in header section marker(<div class="section-marker"></div>)
3. Reproduce current site templates(old site) using redesigned styling for static pages
